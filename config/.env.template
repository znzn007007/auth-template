# ============================================================================
# 通用认证环境变量模板 - Next.js + Supabase
# ============================================================================

# 复制此文件为 .env.local 并填入实际值

# ============================================================================
# Supabase 配置 (必需)
# ============================================================================

# Supabase项目URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase匿名密钥 (public key)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase服务角色密钥 (private key - 仅服务端使用)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# ============================================================================
# 应用配置
# ============================================================================

# 网站基础URL (用于OAuth回调等)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ============================================================================
# OAuth 配置 (可选)
# ============================================================================

# 如果使用自定义OAuth应用，需要配置对应的Client ID和Secret
# 注意：大部分OAuth配置在Supabase后台完成，这里仅作为扩展选项

# Google OAuth (可选)
# GOOGLE_CLIENT_ID=your_google_client_id
# GOOGLE_CLIENT_SECRET=your_google_client_secret

# GitHub OAuth (可选)
# GITHUB_CLIENT_ID=your_github_client_id
# GITHUB_CLIENT_SECRET=your_github_client_secret

# ============================================================================
# 邮件配置 (可选)
# ============================================================================

# 如果使用自定义邮件服务
# EMAIL_FROM=noreply@yourdomain.com
# SMTP_HOST=your_smtp_host
# SMTP_PORT=587
# SMTP_USER=your_smtp_user
# SMTP_PASSWORD=your_smtp_password

# ============================================================================
# 安全配置 (可选)
# ============================================================================

# JWT密钥 (如果需要自定义JWT处理)
# JWT_SECRET=your_jwt_secret_key

# 加密密钥 (如果需要额外的数据加密)
# ENCRYPTION_KEY=your_encryption_key

# ============================================================================
# 开发配置 (仅开发环境)
# ============================================================================

# 是否启用调试模式
# DEBUG=true

# 是否跳过邮箱验证 (仅开发环境)
# SKIP_EMAIL_VERIFICATION=true

# 开发环境数据库URL (如果使用本地数据库)
# DEV_DATABASE_URL=postgresql://username:password@localhost:5432/dbname

# ============================================================================
# 生产环境配置 (仅生产环境)
# ============================================================================

# 生产环境域名
# PRODUCTION_URL=https://yourdomain.com

# 是否启用分析
# ENABLE_ANALYTICS=true

# 错误报告服务 (如 Sentry)
# SENTRY_DSN=your_sentry_dsn

# ============================================================================
# 使用说明
# ============================================================================

# 1. 在Supabase后台获取以下信息：
#    - Project URL (NEXT_PUBLIC_SUPABASE_URL)
#    - Anon/Public Key (NEXT_PUBLIC_SUPABASE_ANON_KEY)
#    - Service Role Key (SUPABASE_SERVICE_ROLE_KEY)

# 2. 配置OAuth提供商（在Supabase Auth设置中）：
#    - Google: 需要在Google Cloud Console创建OAuth应用
#    - GitHub: 需要在GitHub Settings创建OAuth应用
#    - 其他提供商：参考Supabase文档

# 3. 设置重定向URL（在OAuth应用配置中）：
#    - 开发环境: http://localhost:3000/auth/callback
#    - 生产环境: https://yourdomain.com/auth/callback

# 4. 在Supabase Auth设置中配置：
#    - Site URL: 与 NEXT_PUBLIC_SITE_URL 保持一致
#    - Redirect URLs: 添加所有可能的回调URL

# ============================================================================
# 安全注意事项
# ============================================================================

# 1. 永远不要将 .env.local 文件提交到版本控制
# 2. SUPABASE_SERVICE_ROLE_KEY 具有管理员权限，务必保密
# 3. 生产环境使用强密码和加密连接
# 4. 定期轮换密钥和令牌
# 5. 使用环境变量管理服务（如 Vercel Env Variables）